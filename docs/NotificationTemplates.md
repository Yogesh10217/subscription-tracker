# Notification Templates Guide — SubPulse

## Template Catalog
`TemplateService` renders deterministic, escaped HTML/text templates:
- `renderRenewalReminder`
- `renderTrialExpiration`
- `renderPriceChange`
- `renderSubscriptionCreated`
- `renderSubscriptionCancelled`
